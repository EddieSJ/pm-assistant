# 项目管理智能助手 — 技术架构

> 版本：v0.1.0 ｜ 更新时间：2026-09-13｜对应提交：6775985 ｜ 文档性质：技术架构说明书（与代码同仓，作为资产沉淀）
> 项目定位：私人本地部署的项目管理智能体 Web 平台，通过外部 env 接入大模型 API，自动识别项目文件夹中的各类文档，提取关键信息并沉淀到本地记忆体，实现「有据可依、有理有据、有记忆、有标准」的项目管理中枢。

---

## 1. 技术栈总览

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | React 18 + Vite 5 | SPA，构建产物由后端托管 |
| 前端可视化 | Recharts | 柱状图 / 饼图仪表盘 |
| 前端 Markdown | react-markdown + remark-gfm | 聊天回答的富文本渲染 |
| 后端 | Python FastAPI | 单进程托管 API + 前端静态产物 |
| 运行时 | Python 3.14 | 依赖以 requirements.txt 的 >= 宽松约束声明，未锁定（无 lock 文件） |
| 数据校验 | Pydantic v2 | 请求体模型 |
| 数据库 | SQLite（标准库 sqlite3） | 本地单文件，零外部依赖 |
| 大模型 | 阿里云百炼 DashScope 千问 qwen-plus | OpenAI 兼容接口 |
| HTTP 客户端 | httpx | 同步调用 + 流式读取 |
| 文件解析 | PyMuPDF (fitz) / python-docx / openpyxl | PDF / Word / Excel |
| 配置加载 | python-dotenv | 读取本地 `.env` |
| Word 生成 | python-docx | Markdown → .docx |

---

## 2. 系统架构

采用「前端 — 后端 — 数据 — 模型」四层结构，单端口（8000）部署：

```
┌─────────────────────────────────────────────────────────┐
│  浏览器（React 18 + Vite 5 构建产物）                      │
│  ┌──────────┬──────────┬──────────┬──────────────────┐  │
│  │ Sidebar  │ProjectList│ProjectBoard│ ChatPanel/Dash │  │
│  └────┬─────┴────┬─────┴────┬─────┴────────┬─────────┘  │
│       │        fetch /api/*  （JSON）                     │
└───────┼──────────┼──────────┼──────────────┼─────────────┘
        ▼          ▼          ▼              ▼
┌─────────────────────────────────────────────────────────┐
│  FastAPI（main.py，端口 8000，托管前端 dist + API）        │
│  ┌─────────────── routers ───────────────┐               │
│  │ projects │ analysis │ chat │ docs     │               │
│  └───────────────┬───────────────────────┘               │
│        ┌─────────┴─────────┐                              │
│        ▼                   ▼                              │
│  ┌─────────────┐   ┌──────────────────────────────┐      │
│  │ agent.py    │   │ 业务层：knowledge / memory /  │      │
│  │ 找核写追+门 │   │ doc_gen / parsers            │      │
│  └──────┬──────┘   └──────────────┬───────────────┘      │
│         │                         │                       │
│         ▼                         ▼                       │
│  ┌────────────────────┐  ┌────────────────────┐          │
│  │ llm_client.py      │  │ db.py（SQLite 12 表）│          │
│  │ 千问 OpenAI 兼容    │  │ 线程安全读写         │          │
│  └─────────┬──────────┘  └────────────────────┘          │
└────────────┼─────────────────────────────────────────────┘
             ▼
   ┌───────────────────────────┐
   │ DashScope 千问 qwen-plus    │  （外部 API，仅存本地 .env）
   └───────────────────────────┘

 课程资料根目录（PM_ASSISTANT_BASE_DIR 指向）
  ├── 02_千问API与env配置/.env   ← 环境变量 / API Key（绝不入仓）
  ├── 03_本地知识库/            ← 企业知识（注入系统提示）
  ├── 04_文件处理约束库/         ← 文件处理约束（注入系统提示）
  ├── 05_当前任务记忆体/         ← memory_schema + 任务记忆 JSON
  └── 06_项目输入资料包/         ← 待识别的项目原始文件
```

**关键设计原则**：代码与数据分离。`pm-assistant/` 只放代码与本文档；课程资料（`.env`、知识库、约束库、记忆体、输入包）放在上一级目录，运行时通过环境变量 `PM_ASSISTANT_BASE_DIR` 注入。

---

## 3. 目录结构

```
pm-assistant/
├── backend/                 # FastAPI 后端
│   ├── main.py              # 入口：CORS + 路由注册 + /api/health + 托管 dist
│   ├── config.py            # 读取 .env，解析路径，暴露全局 Config
│   ├── db.py                # SQLite 建表 + 线程安全连接管理
│   ├── llm_client.py        # 千问调用（非流式 / 流式 / JSON 稳健提取）
│   ├── parsers.py           # .md/.pdf/.docx/.xlsx/.txt → 纯文本
│   ├── knowledge.py         # 知识库 + 约束库 .md 加载与拼接
│   ├── memory.py            # 三层记忆（工作 / 任务 / 长期）
│   ├── agent.py             # Agent 编排核心（找核写追 + 四道门 + 授权停下）
│   ├── doc_gen.py           # Markdown → Word 生成
│   ├── requirements.txt      # Python 依赖（>= 宽松约束）
│   ├── data/                # 本地 SQLite（pm_assistant.db）
│   └── routers/             # API 路由
│       ├── projects.py      #   项目 CRUD + 本地目录浏览
│       ├── analysis.py      #   文件识别（分析）+ 看板数据
│       ├── chat.py          #   智能聊天 + 历史
│       └── docs.py          #   文档生成 / 下载 / 删除
├── frontend/                # React 前端
│   ├── src/
│   │   ├── App.jsx          # 应用外壳（视图切换）
│   │   ├── api.js           # fetch 封装
│   │   ├── main.jsx         # React 入口
│   │   ├── styles.css       # 全局样式
│   │   └── components/
│   │       ├── Sidebar.jsx       # 侧边栏
│   │       ├── ProjectList.jsx   # 项目列表 + 全局统计
│   │       ├── ProjectModal.jsx  # 新建/编辑项目 + 目录选择
│   │       ├── ProjectBoard.jsx  # 项目看板（9 标签页）
│   │       ├── Dashboard.jsx     # 概览看板（KPI + 图表）
│   │       └── ChatPanel.jsx     # 项目智能助手聊天
│   ├── dist/                # 构建产物（由 FastAPI 托管）
│   ├── index.html           # SPA 入口 HTML
│   ├── package.json         # 前端依赖与脚本
│   └── vite.config.js
├── start.sh                 # 一键启动（自动注入 PM_ASSISTANT_BASE_DIR）
├── README.md
├── 技术架构.md               # 本文档
└── 项目需求说明书.md          # 功能与需求资产文档
```

---

## 4. 后端模块设计

### 4.1 config.py — 配置层
- 用 `BACKEND_DIR.parent` 定位项目根目录；`BASE_DIR` 默认 `parent.parent`，可用环境变量 `PM_ASSISTANT_BASE_DIR` 显式覆盖（支持代码与资料分离）。
- `.env` 优先从 `BASE_DIR/.env` 读取，回退旧结构 `02_千问API与env配置/.env`。
- 暴露 `Config` 类：LLM 参数（api_key / base_url / model / temperature / max_output_tokens）、行为开关（source_trace / require_human_approval）、目录配置（知识库 / 约束库 / 记忆 / 输入包 / 输出 / 日志 / 数据）。
- `ensure_dirs()` 在 import 时创建运行时目录；`has_api_key()` 供 health 接口判断连通性。

### 4.2 db.py — 数据层
- 标准库 `sqlite3`，`check_same_thread=False` + `threading.Lock` 保证后台线程与请求线程并发安全。
- `_SCHEMA` 一次性建 12 张表；`_migrate()` 做向后兼容的轻量字段补充。
- 提供 `query()`（读，返回 dict 列表）与 `execute()`（写，返回 lastrowid）两个统一入口。

### 4.3 llm_client.py — 模型接入
- 对接 DashScope 千问 OpenAI 兼容接口（`/chat/completions`）。
- `chat()` 非流式；`chat_stream()` 流式逐块 yield（当前全仓无调用方，前端走非流式 POST /chat，保留供后续接入流式输出）；`extract_json()` 稳健剥除代码围栏与前后缀后解析 JSON（容忍 LLM 输出噪声）。
- API Key 仅从 `Config.api_key`（环境变量）读取，绝不落日志。

### 4.4 parsers.py — 文件解析
- `parse_file()` 按扩展名分派：`.md` 直读、`.pdf` PyMuPDF、`.docx` python-docx（段落 + 表格）、`.xlsx` openpyxl（逐工作表逐行）。
- `SUPPORTED_EXTS = {.md, .pdf, .docx, .xlsx, .txt}`。对 .md/.pdf/.docx/.xlsx 之外的扩展名走 read_text 兜底（这正是 .txt 能工作的原因）。
- 前后端支持的扩展名清单需保持同步（后端 parsers.SUPPORTED_EXTS，前端 ProjectModal.SUPPORTED）。

### 4.5 knowledge.py — 知识注入
- 加载 `03_本地知识库` 与 `04_文件处理约束库` 下所有 `.md`，拼接为系统提示文本。
- `knowledge_text()` / `constraints_text()` 供 `agent.build_system_prompt()` 使用。

### 4.6 memory.py — 三层记忆
| 记忆层 | 表 | 用途 |
|---|---|---|
| 工作记忆 | `memories_working` | 仅写入 memories_working，当前未回读参与下一轮上下文（跨轮记忆待实现） |
| 任务记忆 | `memories_task` | 对齐 `memory_schema.json`，并 `sync_task_memory_to_file()` 回写 JSON |
| 长期记忆 | `memories_longterm` | 沉淀的项目知识 / 结论 / 标准（key-value） |

### 4.7 agent.py — Agent 编排核心
- `build_system_prompt()`：注入企业知识库 + 文件处理约束库 + 四道门 + 授权-停下 + 输出纪律。
- `_analyze_single_file()`：单文件 → LLM 识别类型 + 提取结构化信息（facts / stakeholders / finance / actions / conflicts），失败时返回带冲突记录的兜底结构，不抛断流水线。
- 单文件注入 LLM 有 `MAX_FILE_TEXT = 12000` 字符截断上限（超出部分截断并追加提示）。
- `_analyze_worker()`：后台线程遍历项目文件夹，逐文件分析并写入各表；结束后综合项目类型、重建任务记忆。
- 分析为「追加写入」模式——_analyze_worker 对各表仅 INSERT，不清理旧记录也不去重；重复点击「重新识别文件」会累积重复数据，当前版本需先删除项目重建才能清零。
- `answer()`：聊天回答，执行「找 → 核 → 写 → 追」全链路。
- 分析进度保存在进程内内存字典 `_jobs`，服务重启即丢失、不跨 worker（多进程）共享，当前按单进程部署设计。
- `sanitize()`：正则过滤身份证 / 手机号 / 银行卡 / API Key 四类敏感信息。

### 4.8 doc_gen.py — 文档生成
- `markdown_to_word()`：按标题（#/##/### → heading 1/2/3）、列表（- → List Bullet，1. → List Number）逐行转 `.docx`；Markdown 表格行（| 开头）按纯文本段落原样保留，不生成 Word 表格结构。
- `build_summary_markdown()`：基于看板数据生成项目综合整理报告 Markdown。

### 4.9 routers/ — API 路由
四组路由均以 `/api/projects` 为前缀（详见第 7 节接口清单）。

---

## 5. 数据模型（12 张表）

| 表 | 用途 | 关键字段 |
|---|---|---|
| `projects` | 项目主表 | name / folder_path / project_type / project_code / description |
| `sources` | 来源文件（原文留存，证据追溯） | file / file_type / content |
| `facts` | 事实台账 | category / field / value / status / source / original_excerpt |
| `conflicts` | 冲突 / 待确认事项 | category / description / source_a / source_b / status |
| `actions` | 行动项 | action / owner / due_date / status / source |
| `stakeholders` | 干系人 | name / organization / role / influence / concern / channel / frequency / owner |
| `finance` | 财务信息 | category / item / amount / tax_inclusive / period / source / note |
| `memories_working` | 工作记忆 | session_id / role / content |
| `memories_task` | 任务记忆 | task_id / objective / as_of_date / json_data |
| `memories_longterm` | 长期记忆 | key / value / category |
| `documents` | 生成的 Word 文档 | title / version / file_path |
| `chat_messages` | 聊天记录 | role / content / sources |

> 事实状态枚举：`confirmed`（已确认）/ `provisional`（待核实）/ `needs_confirmation`（待确认）。
> 行动项状态枚举：`open`（进行中）/ `closed`（已关闭）。
> 所有业务表以 `project_id` 普通列关联；删除项目时由应用层（routers/projects.py 的 delete_project）显式按 11 张子表名单 DELETE 级联，数据库层未声明外键约束。

---

## 6. 核心机制

### 6.1 找 · 核 · 写 · 追
1. **找**：检索项目事实、长期记忆、干系人、财务、行动项、冲突，拼装上下文。
2. **核**：结论必须基于已确认事实，严格区分「事实 / 推断 / 待确认」。
3. **写**：调用 LLM 生成回答，标注来源。
4. **追**：行动项追踪 + 记忆沉淀（工作记忆、聊天记录落库，并在分析结束后写入长期记忆『项目类型』）。

### 6.2 证据链
```
文件 → 证据 → 事实 → 判断 → 行动 → 记忆沉淀
```
每个结论标注 `[来源: 文件名]`；`sources` 表留存原文内容用于追溯。单文件注入 LLM 有 `MAX_FILE_TEXT = 12000` 字符截断上限（超出部分截断并追加提示）。

### 6.3 四道门
事实门（有据）→ 结构门（条理）→ 可用性门（可落地）→ 风险门（不越权、标记「需人工复核」）。

### 6.4 授权-停下
缺关键材料 / 结论不可验证 / 权限不足 / 事项未确认 → 主动停下并说明，绝不编造。（该机制是系统提示词（build_system_prompt）层的输出纪律约束，非代码层强制拦截。）

### 6.5 敏感信息过滤
输出前正则过滤身份证（18 位）、手机号、银行卡 / 长数字、API Key（`sk-` 前缀），不写入记忆。

---

## 7. 接口设计（API 清单）

### 健康检查
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 返回 status / model / has_api_key / 行为开关 |

### 项目与目录（routers/projects.py）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/projects` | 项目列表（含 facts/stakeholders/finance/actions/conflicts/sources 计数） |
| GET | `/api/projects/{pid}` | 项目详情 |
| POST | `/api/projects` | 创建项目（校验文件夹存在） |
| PUT | `/api/projects/{pid}` | 更新项目 |
| DELETE | `/api/projects/{pid}` | 删除项目（级联清理子表） |
| GET | `/api/projects/browse/dirs?path=` | 浏览本地目录（返回子目录 + 全部文件，含 ext 字段；受支持扩展名由前端 ProjectModal 的 SUPPORTED 列表过滤） |

### 文件识别与看板（routers/analysis.py）
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/projects/{pid}/analyze` | 启动后台分析任务，返回 job_id |
| GET | `/api/projects/analyze/status/{job_id}` | 查询分析进度（total/done/current/logs/status） |
| GET | `/api/projects/{pid}/board` | 看板全量数据（project + facts + stakeholders + finance + actions + conflicts + sources + documents） |

### 智能聊天（routers/chat.py）
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/projects/{pid}/chat` | 提问，返回 {answer, sources, stopped}（其中 stopped 为预留字段，当前实现恒为 false，agent.answer 未设置任何停下分支） |
| GET | `/api/projects/{pid}/chat/history` | 聊天历史（最近 100 条；session_id 为预留参数，当前 SQL 未按会话过滤） |

### 文档（routers/docs.py）
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/projects/{pid}/documents/generate` | 由标题 + Markdown 内容生成 Word（版本自动递增 v1.0→v1.1） |
| POST | `/api/projects/{pid}/documents/summary` | 由看板数据生成项目综合整理报告 Word |
| GET | `/api/projects/documents/download/{filename}` | 下载 Word |
| DELETE | `/api/projects/documents/{doc_id}` | 删除文档（含磁盘文件） |

---

## 8. 前端架构

- **App.jsx**：以 `view` 状态在「项目列表 / 项目看板」间切换，无路由（单页两级视图）。
- **api.js**：统一 `fetch` 封装，错误抛出后端 `detail` 信息。
- **ProjectList**：项目卡片 + 全局统计卡（项目总数 / 已提取事实 / 干系人 / 行动项）。
- **ProjectModal**：新建 / 编辑项目，内置目录浏览选择器（仅展示受支持扩展名）。
- **ProjectBoard**：9 个标签页（概览看板 / 事实台账 / 干系人 / 财务 / 行动项 / 冲突待确认 / 来源文件 / 文档 / 项目智能助手）；「重新识别文件」轮询后台任务进度条，「生成综合报告」一键生成 Word。
- **Dashboard**：KPI 指标卡（合同总金额 / 干系人 / 行动项 / 冲突 / 已确认事实）+ Recharts 图表（财务柱状图、干系人饼图、事实状态饼图）+ 冲突风险清单（干系人饼图取 Top 8 单位、冲突清单最多显示 12 条）。
- **ChatPanel**：Markdown 渲染回答，标注来源，Enter 发送 / Shift+Enter 换行。
- 前后端支持的扩展名清单需保持同步（后端 parsers.SUPPORTED_EXTS，前端 ProjectModal.SUPPORTED）。

---

## 9. 配置与部署

### 环境变量（.env）
| 变量 | 默认值 | 说明 |
|---|---|---|
| `DASHSCOPE_API_KEY` | — | 千问 API Key（必填，仅存本地） |
| `QWEN_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 兼容接口地址 |
| `QWEN_MODEL` | `qwen-plus` | 模型名 |
| `AGENT_TEMPERATURE` | `0.2` | 生成温度 |
| `AGENT_MAX_OUTPUT_TOKENS` | `12000` | 最大输出 token |
| `AGENT_ENABLE_SOURCE_TRACE` | `true` | 是否启用来源追溯 |
| `AGENT_REQUIRE_HUMAN_APPROVAL` | `true` | 是否要求人工审批（只准备草稿，不自动外发） |
| `AGENT_KNOWLEDGE_DIR` / `AGENT_CONSTRAINT_DIR` / `AGENT_MEMORY_FILE` / `AGENT_INPUT_DIR` / `AGENT_OUTPUT_DIR` / `AGENT_LOG_DIR` | 见 config.py | 课程资料各子目录 |
| `AGENT_DATA_DIR` | — | 本地数据目录（可覆盖） |
| `PM_ASSISTANT_BASE_DIR` | `parent.parent` | 课程资料根目录（代码分离时必填） |
| `PM_ASSISTANT_ENV` | — | 覆盖 .env 文件路径，默认回退 BASE_DIR/.env → 02_千问API与env配置/.env |

### 启动方式
- **生产单端口**：`./start.sh`（自动注入 `PM_ASSISTANT_BASE_DIR`，由 FastAPI 托管 `frontend/dist`，只需 8000 端口）。
- **开发双端口**：后端 `uvicorn main:app --reload`（8000，须先 cd backend，并使用 backend/.venv 中的 uvicorn，因 main.py 依赖相对导入）+ 前端 `npm run dev`（Vite 5173，`/api` 代理）。

---

## 10. 安全设计

1. **凭据隔离**：API Key 仅存于本地 `.env`，绝不硬编码 / 打印 / 落库 / 上传。
2. **敏感信息过滤**：输出前正则隐藏身份证 / 手机号 / 银行卡 / API Key，且不写入记忆。
3. **授权边界**：`AGENT_REQUIRE_HUMAN_APPROVAL=true`，Agent 只准备草稿与整理结果，不自动外发 / 审批。
4. **人工复核标记**：财务 / 合同 / 人事结论一律标记「需人工复核」。
5. **本地化部署**：`localhost` 单端口，数据（SQLite + 资料包）全在本机，不依赖云端存储。
6. **已知待加固**：下载接口 `/api/projects/documents/download/{filename}` 的 filename 未做路径穿越（..）校验，当前依赖本地单用户部署前提。
7. **CORS 放宽**：当前 `allow_origins=["*"]` 全开放，属本地单用户部署前提下的放宽策略，暴露公网前需收紧。

# 项目管理智能助手 — 项目需求说明书

> 版本：v0.1.0 ｜ 更新时间：2026-09-13｜对应提交：6775985 ｜ 文档性质：需求规格说明书（与代码同仓，作为资产沉淀）
> 说明：本说明书中的功能均为**已实现功能**（从现有代码梳理而来）。后续修改代码前，须先更新本说明书，再交由 coder 更新代码。

---

## 1. 引言

### 1.1 编写目的
本文档定义「项目管理智能助手」平台的功能需求、业务规则与约束，作为开发、验收与后续迭代的基准。平台是一个**私人本地部署**的项目管理智能体 Web 应用：通过外部 env 接入大模型 API，自动识别项目文件夹中的各类文档，提取关键信息并沉淀到本地记忆体。

### 1.2 项目范围
- **在范围**：项目全局管理、文件自动识别、项目看板可视化、智能聊天问答、Word 文档生成、三层记忆沉淀。
- **不在范围**：多用户权限体系、云端部署、移动端、向量数据库检索（当前版本不引入）。

### 1.3 目标用户
项目经理（个人使用），服务于虚构企业「上海澄岳智能装备有限公司」的教学场景。

### 1.4 术语定义
| 术语 | 定义 |
|---|---|
| 事实（Fact） | 从项目文件中提取的结构化关键信息，带状态与来源 |
| 证据链 | 文件 → 证据 → 事实 → 判断 → 行动 → 记忆沉淀 的追溯链路 |
| 四道门 | 事实门 → 结构门 → 可用性门 → 风险门 的输出纪律 |
| 授权-停下 | 缺料 / 不可验证 / 权限不足 / 未确认时主动停下，不编造 |
| 三层记忆 | 工作记忆 / 任务记忆 / 长期记忆 |
| 干系人 | 项目相关方（甲方 / 乙方团队、影响方等） |

---

## 2. 项目概述

### 2.1 背景
项目经理面对大量项目文档（标书、合同、计划、团队清单、干系人清单、会议记录、财务资料、问题风险记录），需要快速、准确地掌握项目全貌并支持决策。传统人工整理耗时且易遗漏。

### 2.2 建设目标
构建一个「有据可依、有理有据、有记忆、有标准」的本地项目管理中枢：
1. **有据可依**：所有结论可追溯到具体来源文件与原文。
2. **有理有据**：回答基于已确认事实，不编造。
3. **有记忆**：三层记忆沉淀，杜绝幻觉与重复劳动。
4. **有标准**：遵循企业知识库与文件处理约束库的规范。

### 2.3 技术栈（概要）
React 18 + Vite 5（前端）｜ FastAPI（后端）｜ SQLite（数据库）｜ 阿里云百炼 DashScope 千问 qwen-plus（大模型）。详见《技术架构.md》。

---

## 3. 功能需求

### 3.1 项目全局管理
| 编号 | 功能 | 描述 | 状态 |
|---|---|---|---|
| F-01 | 项目创建 | 填写项目名称、选择 / 输入项目文件夹路径、可选描述，创建项目 | ✅ 已实现 |
| F-02 | 项目查询 | 列表展示全部项目，含类型、描述、各维度数据计数、更新时间 | ✅ 已实现 |
| F-03 | 项目编辑 | 修改项目名称 / 文件夹路径 / 描述 | ✅ 已实现 |
| F-04 | 项目删除 | 删除项目，级联清理该项目下的全部识别数据与记忆 | ✅ 已实现 |
| F-05 | 全局统计 | 顶部统计卡展示项目总数、已提取事实、干系人、行动项 | ✅ 已实现 |
| F-06 | 本地目录浏览 | 后端提供目录浏览接口，前端选择项目文件夹（仅展示受支持扩展名，隐藏系统隐藏目录） | ✅ 已实现 |

### 3.2 文件自动识别（后台分析）
| 编号 | 功能 | 描述 | 状态 |
|---|---|---|---|
| F-07 | 多格式解析 | 解析 `.md` / `.pdf` / `.docx` / `.xlsx` / `.txt` 为纯文本 | ✅ 已实现 |
| F-08 | 文件类型识别 | LLM 自动区分文件类型（招标 / 投标 / 合同 / 计划 / 团队 / 干系人 / 会议邮件 / 财务 / 问题风险 / 其他），无需人工告知 | ✅ 已实现 |
| F-09 | 结构化信息提取 | 从每个文件提取事实（facts）、干系人（stakeholders）、财务（finance）、行动项（actions）、冲突（conflicts） | ✅ 已实现 |
| F-10 | 后台异步执行 | 分析任务在后台线程运行，前端轮询进度条（当前文件 / 已完成数 / 总数）；分析为追加写入模式，重复识别会累积重复记录，需删除项目重建才能清零 | ✅ 已实现 |
| F-11 | 项目类型综合判断 | 汇总各文件类型提示，由 LLM 综合判断统一项目类型 | ✅ 已实现 |
| F-12 | 原文留存 | 每个来源文件内容留存 `sources` 表，支持证据追溯；单文件注入 LLM 上限 12000 字符，超出截断 | ✅ 已实现 |
| F-13 | 容错兜底 | 单文件解析 / 识别失败不中断整条流水线，记录为冲突项 | ✅ 已实现 |

### 3.3 项目看板
| 编号 | 功能 | 描述 | 状态 |
|---|---|---|---|
| F-14 | 概览看板 | KPI 指标卡（合同总金额 / 干系人 / 行动项 / 冲突待确认 / 已确认事实）+ 项目核心信息 | ✅ 已实现 |
| F-15 | 财务金额分布 | 柱状图展示财务金额按类别分布 | ✅ 已实现 |
| F-16 | 干系人分布 | 饼图展示干系人按单位分布（干系人取 Top 8 单位） | ✅ 已实现 |
| F-17 | 事实确认度 | 饼图展示事实状态分布（已确认 / 待核实 / 待确认） | ✅ 已实现 |
| F-18 | 冲突风险提示 | 清单展示冲突与待确认事项（类别 + 描述；冲突清单最多显示 12 条） | ✅ 已实现 |
| F-19 | 事实台账 | 表格展示事实（分类 / 字段 / 值 / 状态 / 来源） | ✅ 已实现 |
| F-20 | 干系人明细 | 表格展示干系人（姓名 / 单位 / 角色 / 影响力 / 关注点 / 沟通渠道） | ✅ 已实现 |
| F-21 | 财务明细 | 表格展示财务（类别 / 项目 / 金额 / 口径 / 来源 / 备注），标注需财务复核 | ✅ 已实现 |
| F-22 | 行动项明细 | 表格展示行动项（行动 / 负责人 / 期限 / 状态 / 来源） | ✅ 已实现 |
| F-23 | 冲突 / 待确认明细 | 表格展示冲突（类别 / 描述 / 来源 A / 来源 B） | ✅ 已实现 |
| F-24 | 来源文件清单 | 表格展示已加载文件（文件名 / 识别类型 / 加载时间） | ✅ 已实现 |

### 3.4 智能聊天（项目智能助手）
| 编号 | 功能 | 描述 | 状态 |
|---|---|---|---|
| F-25 | 基于事实问答 | 回答基于已识别的项目事实 + 长期记忆 + 干系人 + 财务 + 行动项 + 冲突/待确认事项 | ✅ 已实现 |
| F-26 | 来源标注 | 每个关键结论标注 `[来源: 文件名]`，回答下方展示来源文件列表 | ✅ 已实现 |
| F-27 | Markdown 渲染 | 回答以 Markdown 富文本渲染（含表格 / 列表） | ✅ 已实现 |
| F-28 | 聊天历史 | 持久化聊天记录，重进页面可加载历史 | ✅ 已实现 |
| F-29 | 工作记忆 | 问答内容写入工作记忆（memories_working），当前未回读参与下一轮上下文（跨轮记忆待实现） | ✅ 已实现 |

### 3.5 文档生成
| 编号 | 功能 | 描述 | 状态 |
|---|---|---|---|
| F-30 | 综合整理报告 | 基于看板数据（事实 / 干系人 / 财务 / 行动项 / 冲突）一键生成 Word 报告 | ✅ 已实现 |
| F-31 | 自定义文档生成 | 由标题 + Markdown 内容生成 Word | ✅ 已实现 |
| F-32 | 版本管理 | 同标题文档版本自动递增（v1.0 → v1.1 → v1.2 …） | ✅ 已实现 |
| F-33 | 文档下载 | 生成的 Word 可在线下载 | ✅ 已实现 |
| F-34 | 文档删除 | 删除文档记录与磁盘文件 | ✅ 已实现 |
| F-35 | 文档清单展示 | 按项目列出已生成文档的标题 / 版本号 / 生成时间，并提供下载与删除入口（对应 ProjectBoard 的 DocumentsPanel） | ✅ 已实现 |

### 3.6 三层记忆
| 编号 | 功能 | 描述 | 状态 |
|---|---|---|---|
| F-36 | 工作记忆 | 会话上下文（memories_working 表） | ✅ 已实现 |
| F-37 | 任务记忆 | 对齐 `memory_schema.json`，分析完成后重建，并回写 `current_task_memory.json` | ✅ 已实现 |
| F-38 | 长期记忆 | 沉淀项目知识 / 结论 / 标准（如「项目类型」），供后续回答引用 | ✅ 已实现 |

---

## 4. 已实现功能清单（总表）

| 模块 | 功能数 | 核心功能 |
|---|---|---|
| 项目全局管理 | 6（F-01~F-06） | 增删改查、目录浏览、全局统计 |
| 文件自动识别 | 7（F-07~F-13） | 多格式解析、类型识别、结构化提取、异步执行、容错 |
| 项目看板 | 11（F-14~F-24） | 概览 KPI + 图表 + 7 类明细台账 |
| 智能聊天 | 5（F-25~F-29） | 基于事实问答、来源标注、Markdown、历史 |
| 文档生成 | 6（F-30~F-35） | 综合报告、自定义文档、版本管理、下载删除、文档清单 |
| 三层记忆 | 3（F-36~F-38） | 工作 / 任务 / 长期记忆 |

**合计：38 项功能已实现。**

---

## 5. 非功能需求

| 编号 | 类别 | 需求 |
|---|---|---|
| N-01 | 部署形态 | 本地 localhost 单端口部署，不依赖云端存储 |
| N-02 | 数据本地化 | 项目数据与记忆全部落本地 SQLite + 本地文件 |
| N-03 | 凭据安全 | API Key 仅存本地 `.env`，绝不硬编码 / 打印 / 落库 / 上传 |
| N-04 | 敏感信息 | 身份证 / 手机号 / 银行卡 / API Key 输出前隐藏，不写入记忆 |
| N-05 | 授权边界 | Agent 只准备草稿与整理结果，不自动外发 / 审批 |
| N-06 | 可追溯性 | 每个结论可追溯到来源文件与原文 |
| N-07 | 容错性 | 单文件失败不中断整体分析，以冲突项兜底 |
| N-08 | 并发安全 | 后台分析线程与请求线程共享 SQLite，由 threading.Lock 串行化写；分析进度为进程内内存态（_jobs），非持久化、不跨 worker |

---

## 6. 数据需求

### 6.1 数据来源
课程资料目录（`PM_ASSISTANT_BASE_DIR` 指向）下的：
- `02_千问API与env配置`（含 千问API配置说明.md 与生效 .env）
- `03_本地知识库`（企业概况 / 文化 / 业务 / 组织架构 / 管理制度等 9 份）
- `04_文件处理约束库`（来源优先级 / 招标投标 / 合同 / 计划 / 会议 / 财务 / 干系人 / 任务记忆 8 份约束）
- `05_当前任务记忆体`（memory_schema.json + 任务记忆 JSON）
- `06_项目输入资料包`（招标 / 投标 / 合同 / 计划 / 团队清单 / 干系人清单 / 会议记录 / 财务 / 问题风险 9 份样本）

### 6.2 数据实体
见《技术架构.md》第 5 节（12 张表）。

---

## 7. 业务规则与约束

### 7.1 输出纪律（四道门）
1. **事实门**：结论基于已确认事实 / 证据，标注来源，区分「事实 / 推断 / 待确认」。
2. **结构门**：结构完整、条理清晰，遵循约束库格式要求。
3. **可用性门**：输出可直接用于决策与执行。
4. **风险门**：不越权、不替授权审批，财务 / 合同 / 人事结论标记「需人工复核」。

### 7.2 授权-停下
遇到以下任一情况必须主动停下并说明，绝不编造：
- 缺关键材料；结论不可验证；权限不足；事项未确认。

### 7.3 输出要求
- 每个关键结论标注来源，格式 `[来源: 文件名/章节]`。
- 财务金额保留原始口径（含税 / 不含税、当期 / 累计）。
- 人名保留原文，疑似错字不得擅自合并。
- 不得生成不存在的人员、联系方式、金额、日期、审批或签章。

### 7.4 事实状态判定
- 文件明确写明的 → `confirmed`（已确认）。
- 合理推测 → `provisional`（待核实）。
- 无法确定 → `needs_confirmation`（待确认）。

---

## 8. 后续迭代约定

> 以下为项目维护的固定工作流，团队成员（含 Agent 角色）须遵守。

1. **先文档后代码**：任何功能变更，先更新本《项目需求说明书》，再更新代码。
2. **代码交给 coder**：代码层面的改动由 coder 角色负责实现，不越过该角色直接改代码。
3. **资产同仓**：技术架构、需求说明书与代码同仓维护，保持同步。
